apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontendDep
  labels:
    app: frontendDep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: thienpvt/frontend
          ports:
            - name: frontend-port
              containerPort: 80